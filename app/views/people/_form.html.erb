<%= simple_form_for @person do |p| %>
  <%= p.input :first_name %>
  <%= p.input :last_name %>
  <%= p.input :picture, as: :file %>
  <%= p.input :bio %>
  <%= p.input :birthdate, as: :date, start_year: Date.today.year - 100 %>
  <%= p.button :submit %>
<% end %>
