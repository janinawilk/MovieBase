<%= simple_form_for @person do |p| %>
  <%= p.input :first_name %>
  <%= p.input :last_name %>
  <%= p.input :picture, as: :file %>
  <%= p.input :bio %>
  <%= p.input :birthdate, as: :date %>
  <%= p.button :submit %>
<% end %>
